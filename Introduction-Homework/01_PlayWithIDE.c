#include <stdio.h>
#include <stdlib.h>


// If using Netbeans you need to right-click each source
// and click on "Excluded from build" to be able to build
// and run each file successfuly.


int main()
{
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("%d %d", a, b);
    return 0;
}

